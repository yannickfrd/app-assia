<!DOCTYPE html>
<html lang="fr">
    <head>
        <title>{{ title }}</title>
        <meta charset='utf-8' />
            <style type='text/css'>
                html {
                    margin: 0;
                }
                body {
                    margin: {{ margin is defined ? margin : 0 }};
                }
                .container {
                    display: flex;
                }
                img {
                    width: 100%!important;
                    max-width: 100%;
                    max-height: 100%;
                    position: absolute;
                    top: 50%;
                    left: 50%;
                    transform: translate(-50%, -50%);
                }
            </style>
    </head>
    <body>
        <div class="container">
            <img src="{{ base64 }}" width="1024"/>
        </div>
    </body>
</html>
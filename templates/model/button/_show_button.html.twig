{% set disabled = disabled is defined and disabled == true ? 'disabled' : '' %}
<a href="{{ path_route }}"
    class="btn btn-{{ disabled ? 'secondary' : theme_color }} btn-sm shadow {{ disabled }}"
    title="{{ title is defined ? title : 'Voir l\'élément' }}" type="button"
    data-toggle="tooltip" data-placement="bottom" data-action="show">
    <i class="fas fa-eye"></i>
</a>
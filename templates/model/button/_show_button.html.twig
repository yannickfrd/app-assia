{% set disabled = disabled is defined and disabled == true ? 'disabled' : '' %}
<a href="{{ path ?? '' }}"
    class="btn btn-{{ disabled ? 'secondary disabled' : 'primary' }} btn-sm shadow"
    title="{{ show_title is defined ? show_title : 'Voir l\'élément' }}" type="button"
    data-bs-toggle="tooltip" data-bs-placement="bottom" data-action="show">
    <i class="fas fa-eye"></i>
</a>
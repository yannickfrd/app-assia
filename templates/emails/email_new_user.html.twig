{% extends "emails/base_email.html.twig" %}

{% block greeting %}
    <p class="mb-4">Bonjour {{ user.firstname }}, </p>
{% endblock %}

{% block body %}
    <p>Un compte utilisateur a été créé pour vous sur l'application {% if app_version != "prod" %}<b><u>DEMO</u></b> {% endif %}
        <b><a href="{{ url('index') }}">Assia</a></b>
    </p>

    {% if app_version != "prod" %}
        <p>Attention, il s'agit de la version test de l'application. 
            Les données enregistrées ici ne le seront pas dans la base de données réelle.</p>
    {% endif %}

    <p>Votre login : <b>{{ user.username }}</b></p>
    <p>Votre email : <b>{{ user.email }}</b></p>

    <p>Veuillez cliquer sur <a href="{{ url('security_create_password', {'token':user.token}) }}">ce lien</a> 
    pour définir votre mot de passe.</p>
    <p>Ce lien est valide 24 heures.</p>
{% endblock %}
{% extends 'model/_support_search_form.html.twig' %}

{% block search_form %}
    <div class="col-auto my-1 px-1">{{ form_widget(form_search.title) }}</div>
    <div class="col-auto d-inline-flex my-1 px-1 align-items-center">
        <span class="pe-2">Entre</span>{{ form_widget(form_search.date.start) }}
    </div>
    <div class="col-auto d-inline-flex  my-1 px-1 align-items-center">
        <span class="pe-2">et</span>{{ form_widget(form_search.date.end) }}
    </div>
    <div class="col-auto my-1 px-1">
        <div class="input-group">
            <span class="input-group-text" title="Étiquettes" data-toggle="tooltip" data-placement="bottom">
                <i class="fas fa-tag text-black-50"></i>
            </span>
            {{ form_widget(form_search.tags) }}
        </div> 
    </div>
{% endblock %}

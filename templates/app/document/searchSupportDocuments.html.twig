{% extends "model/supportSearch.html.twig" %}

{% block search_form %}
    <div class="form-group col-auto my-1 px-1">{{ form_row(form_search.name) }}</div>
    <div class="form-group col-auto my-1 px-1">{{ form_row(form_search.type) }}</div>
{% endblock %}
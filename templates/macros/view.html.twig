{#  #}
{% macro view(value, key = null, trans = "forms") %}
    {% if value != "" and value != "Non renseigné" %}
        <div class="col-md-6 mb-1 py-2">
        {% if key is not null %}
            {# <div class="col-4 col-md-2 pr-0 py-2 text-secondary">{{ key|trans({}, trans) }} : </div> #}
            <span class="py-2 text-secondary">{{ key|trans({}, trans) }} : </span>
        {% endif %}
        {# <div class="col-8 col-md-4 pl-4 mb-1 py-2">{{ value }}</div> #}
        <span>{{ value }}</span>
        </div>
    {% endif %}
{% endmacro %}